# SPDX-License-Identifier: Apache-2.0

menu "SPI Uptime Exchange"

choice ROLE_SELECTION
	prompt "Node role"
	default ROLE_WORKER

config ROLE_WORKER
	bool "Worker (initiates SPI transfers)"

config ROLE_CHILD
	bool "Child (waits for worker clock)"

endchoice

endmenu

source "Kconfig.zephyr"
